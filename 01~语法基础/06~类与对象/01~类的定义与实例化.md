# Python 类与对象系统教程

## 1. 面向对象编程（OOP）简介

- 什么是面向对象编程
- OOP 的三大特性：封装、继承、多态
- Python 中的对象和类

## 2. 类的定义与实例化

- 使用 `class` 关键字定义类
- `__init__` 构造方法
- 创建对象（实例化）
- 实例属性与类属性

**示例：**

```python
class Dog:
    species = "Canis familiaris"  # 类属性

    def __init__(self, name, age):
        self.name = name  # 实例属性
        self.age = age

dog1 = Dog("Buddy", 3)
```

## 3. 方法

- 实例方法（self）
- 类方法（@classmethod, cls）
- 静态方法（@staticmethod）

**示例：**

```python
class MyClass:
    def instance_method(self):
        pass

    @classmethod
    def class_method(cls):
        pass

    @staticmethod
    def static_method():
        pass
```

## 4. 属性与封装

- 公有属性与私有属性（单下划线、双下划线）
- 属性访问控制
- 使用 `@property` 装饰器实现 getter/setter

**示例：**

```python
class Person:
    def __init__(self, name):
        self._name = name  # 受保护属性

    @property
    def name(self):
        return self._name

    @name.setter
    def name(self, value):
        self._name = value
```

## 5. 继承与多态

- 单继承与多继承
- 方法重写（Override）
- 使用 `super()`
- 多态的实现

**示例：**

```python
class Animal:
    def speak(self):
        pass

class Dog(Animal):
    def speak(self):
        print("Woof!")

class Cat(Animal):
    def speak(self):
        print("Meow!")

def animal_speak(animal):
    animal.speak()
```

## 6. 魔法方法（Magic Methods）

- `__str__`, `__repr__`
- 运算符重载（如 `__add__`, `__eq__`）
- `__len__`, `__getitem__`, `__setitem__` 等

**示例：**

```python
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y

    def __add__(self, other):
        return Vector(self.x + other.x, self.y + other.y)

    def __str__(self):
        return f"Vector({self.x}, {self.y})"
```

## 7. 类的高级用法

- 抽象类（abc 模块）
- 混入（Mixin）
- 组合（Composition）与聚合（Aggregation）

## 8. 类的应用实例

- 设计模式中的类（如单例、工厂、观察者等）
- 实际项目中的类设计案例

## 9. 常见问题与最佳实践

- 何时使用类，何时用函数
- 类的命名规范
- 代码可读性与可维护性
